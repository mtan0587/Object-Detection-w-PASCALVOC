# Windows/WSL zone identifiers
*.Identifier

# Python virtual environment
.venv/

# Large dataset archives and raw VOC directory (re-download via notebook cells 1-4)
*.tar
VOCdevkit/

# Dataset symlink directory (regenerated by notebook cell 5)
voc_dataset/

# Model weight files (too large for git)
*.pt

# Training run outputs (weights, plots, logs)
runs/

# voc.yaml uses absolute paths — regenerated by notebook cell 5
voc.yaml

# Draft / Colab notebook
Assignment_3_1_NANO.ipynb

# Archive files
*.zip

# Claude assistant context
.claude/

# Rejected model directories (never used)
ScratchDet-master/
nanodet-main/
YOLOX-main/
yolov5g/